<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Node&Mongo - 채팅목록</title>
    <link href="/main.css" rel="stylesheet" />
  </head>
  <body class="grey-bg">
    <%- include("nav.ejs") %>
    <h3 class="header-text">채팅</h3>
    <div class="white-bg">
      <% for (let i = 0; i < result.length; i++) { %>
      <a href="/chat/detail/<%= result[i]._id %>">
        <div class="list-box">
          <h4>
            user<%= (user.toString() == result[i].users[0].toString() ?
            result[i].users[1] : result[i].users[0]) %> <%= console.log(user,
            result[i].users) %>
          </h4>
          <p><%= result[i]._id %></p>
        </div>
      </a>
      <% } %>
    </div>
  </body>
</html>
